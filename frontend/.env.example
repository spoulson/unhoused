# Copy to .env (or .env.local) to override. Vite only picks up variables prefixed with VITE_.

# Default Gruvbox theme mode shown to first-time visitors (before they've toggled it themselves,
# at which point their choice is remembered in the browser and this no longer applies).
# One of: light, dark
VITE_DEFAULT_THEME=light
