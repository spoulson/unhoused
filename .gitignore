/bin/

# Local configs contain plaintext Nomad tokens (specs/configuration.md) — never commit them.
# config.example.yaml is the only config meant to be tracked.
/config.yaml
/config.*.yaml
!/config.example.yaml

/.claude/
